import { PostPortraitInterceptor } from './post-portrait.interceptor';

describe('PostPortraitInterceptor', () => {
  it('should be defined', () => {
    expect(new PostPortraitInterceptor()).toBeDefined();
  });
});
