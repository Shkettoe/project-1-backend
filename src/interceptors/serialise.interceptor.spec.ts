import { SerialiseInterceptor } from './serialise.interceptor';

describe('SerialiseInterceptor', () => {
  it('should be defined', () => {
    expect(new SerialiseInterceptor()).toBeDefined();
  });
});
